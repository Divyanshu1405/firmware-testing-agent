{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "monitor.schema.json",
  "title": "Monitor",
  "type": "object",
  "required": ["monitor_id", "requirement_id", "kind", "oracle_source"],
  "additionalProperties": true,
  "properties": {
    "monitor_id": { "type": "string" },
    "requirement_id": { "type": "string" },
    "kind": {
      "type": "string",
      "enum": ["always", "never", "within", "eventually"]
    },
    "when": {
      "type": "object",
      "required": ["channel", "op", "value"],
      "additionalProperties": true,
      "properties": {
        "channel": { "type": "string" },
        "op": { "type": "string" },
        "value": {},
        "hold_ms": { "type": "integer", "minimum": 0 }
      }
    },
    "then": {
      "type": "object",
      "required": ["channel", "op", "value"],
      "additionalProperties": true,
      "properties": {
        "channel": { "type": "string" },
        "op": { "type": "string" },
        "value": {}
      }
    },
    "within_ms": { "type": "integer", "minimum": 0 },
    "min_interval_ms": { "type": "integer", "minimum": 0 },
    "oracle_source": {
      "type": "string",
      "enum": ["spec", "inferred", "generic"]
    }
  }
}
